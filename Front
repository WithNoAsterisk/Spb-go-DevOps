FROM node:16
WORKDIR /frontend
COPY ./ .
RUN apt-get update && apt-get upgrade -y && \
    apt-get install -y nodejs \
    npm install --silent && \
    npm install -g serve:
RUN npm run build
EXPOSE 3000
ENTRYPOINT ["serve", "-s", "build"]
